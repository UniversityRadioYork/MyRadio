<?xml version="1.0"  encoding="UTF-8" ?>

<project name="MyURY" basedir="." default="all">
    <property file="./build.properties" />

    <property name="package"  value="${phing.project.name}" override="true" />
    <property name="builddir" value="${project.basedir}/build/" override="true" />
    <property name="srcdir"   value="${project.basedir}/src" override="true" />

    <!-- Fileset for all files -->
    <fileset dir="${srcdir}" id="allfiles">
        <include name="**" />
    </fileset>

    <!-- ============================================  -->
    <!-- (DEFAULT) Target: main                        -->
    <!-- ============================================  -->
    <target name="copyfiles" description="Copies files from src to build">
        <copy todir="${builddir}">
            <fileset refid="allfiles" />
        </copy>
    </target>

    <target name="all" depends="copyfiles">
    </target>
</project>
