{% extends 'base.twig' %}
{% block content %}
<div id="content-stripe" class="clearfix">
  {% for notice in notices %}
    <div class="ui-state-{{notice.state}}"><span class="ui-icon ui-icon-{{notice.icon}}" style="float:left"></span>{{notice.message}}</div>
  {% endfor %}
  {% block stripecontent %}{% endblock %}
</div>
{% endblock %}